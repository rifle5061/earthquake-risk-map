name: Update tsunami data

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: write

jobs:
  update-tsunami-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update tsunami data
        run: |
          python scripts/update_tsunami_data.py --pretty

      - name: Commit updated tsunami data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git pull --rebase origin main || true
          git add data/latest-tsunami.json
          git commit -m "Update tsunami data" || echo "No changes to commit"
          git push
